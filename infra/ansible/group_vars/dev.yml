---
gitlab_url: https://gitlab.com/

gitlab_runner_description: product-catalog-dev-runner

gitlab_runner_executor: docker

gitlab_runner_default_image: docker:27-cli

gitlab_runner_token: "{{ lookup('env', 'GITLAB_RUNNER_TOKEN') }}"
